[project]
name = "authority-cardgame"
version = "0.1.0"
description = "Authority Card Game Documentation"
requires-python = ">=3.8"

[tool.poetry.dependencies]
python = "^3.8"
mkdocs = "^1.5.0"
mkdocs-material = "^9.0.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"